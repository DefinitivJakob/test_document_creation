node_modules/
dist/
*.log
.DS_Store
.env
.env.*

# Test data — large binaries, often confidential. Lives on dev-fixtures branch.
test-cases/

# No .docx files on main (templates + outputs belong on dev-fixtures only)
*.docx

# Azure Functions local secrets
local.settings.json

# Build/runtime artifacts
*.tsbuildinfo
